{
  "name": "tsgo-gql-tada-union-perf",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal repro for tsgo slowdown on gql.tada union-heavy typing",
  "scripts": {
    "schema:generate": "tsx ./scripts/genSchema.ts && npm run types:generate",
    "types:generate": "gql.tada generate-output --tsconfig ./tsconfig.json --output ./src/__generated_schema.d.ts",
    "lint:clear": "rm -f tsconfig.tsbuildinfo tsconfig.tsgo.tsbuildinfo",
    "lint:tsc": "tsc --noEmit --skipLibCheck --incremental --diagnostics",
    "lint:tsgo": "tsgo --noEmit --skipLibCheck --incremental --diagnostics",
    "lint:tsgo:single-threaded": "tsgo --noEmit --skipLibCheck --incremental --singleThreaded --diagnostics",
    "lint:tsgo:checkers": "tsgo --noEmit --skipLibCheck --incremental --checkers 1 --diagnostics",
    "bench:once:tsc": "npm run lint:clear && npm run lint:tsc",
    "bench:once:tsgo": "npm run lint:clear && npm run lint:tsgo",
    "bench:once:tsgo:single-threaded": "npm run lint:clear && npm run lint:tsgo:single-threaded",
    "bench:once:tsgo:checkers": "npm run lint:clear && npm run lint:tsgo:checkers",
    "bench:loop": "node ./scripts/benchLoop.mjs",
    "verify:toolchain": "tsc --version && tsgo --help > /dev/null && tsgo --singleThreaded --help > /dev/null && echo \"tsgo single-threaded flag is available\""
  },
  "devDependencies": {
    "@types/node": "25.3.0",
    "@typescript/native-preview": "7.0.0-dev.20260220.1",
    "gql.tada": "1.9.0",
    "graphql": "16.12.0",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  }
}
